import { View, Text } from 'react-native';

export default function NotasScreen() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Pantalla de Notas de texto</Text>
    </View>
  );
}